# scripts

| Script | Description |
|---|---|
| [bash/lxc-init.sh](bash/README.md) | Bootstrap a fresh Ubuntu 26.04 instance: sudo user, SSH key, key-only sshd |

Tests live in `tests/`, mirroring the script layout (e.g. `tests/bash/test-lxc-init.sh`).
